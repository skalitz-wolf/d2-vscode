# Spec Delta

## ADDED Requirements

### Requirement: 缩放百分比显示与生效比例一致

预览面板 SHALL 让缩放百分比文字反映**当前生效**的缩放比例；任何改变缩放比例的入口（缩放滑块、缩放按钮、Ctrl+滚轮、重置缩放、重新渲染时保留缩放）在操作生效后，百分比文字 MUST 立即与该比例一致，不得显示上一次的值。

#### Scenario: 拖动缩放滑块后百分比立即更新

- **WHEN** 用户把缩放滑块拖到 400%
- **THEN** 百分比文字显示 400%，且预览图按 400% 渲染

#### Scenario: 连续拖动滑块

- **WHEN** 用户连续把滑块拖到多个不同比例（如 400%、500%、120%、750%、10%）
- **THEN** 每次操作后百分比文字都等于刚设定的比例，不发生滞后

#### Scenario: 重置缩放

- **WHEN** 用户触发重置缩放（扩展端的重置入口）
- **THEN** 百分比文字显示 100%，且预览图按 100% 渲染

#### Scenario: 保留缩放的重新渲染

- **WHEN** 预览已处于某个缩放比例，触发一次保留缩放的重新渲染
- **THEN** 百分比文字仍等于该比例，不发生跳变

### Requirement: 缩放操作不因入参类型而中断预览

预览面板 SHALL 接受数字与数字字符串两种形式的缩放比例输入（缩放滑块与扩展端消息均以字符串传递），并 MUST NOT 因入参类型导致未捕获异常。

- 未捕获异常会中断预览页脚本，连带使缩放、平移、board 跳转、场景栏全部失效，因此 MUST 避免。

#### Scenario: 字符串比例不引发异常

- **WHEN** 缩放入口以字符串形式传入比例（如滑块值 `"400"`）
- **THEN** 预览正常缩放到该比例，百分比文字正确，页面无未捕获异常

#### Scenario: 字符串比例后继续操作

- **WHEN** 以字符串形式设置过一次缩放比例后，再次执行任意缩放操作
- **THEN** 操作正常完成，预览页未因上一次的入参类型而中断

#### Scenario: 缩放状态保持数值类型

- **WHEN** 通过任一入口设置缩放比例后读取当前缩放比例
- **THEN** 其类型为数值，可参与后续算术与格式化
