# Tasks

## 1. 文字复制（核心）

- [x] 1.1 在 `pages/previewPage.html` 增加文本提取函数：元素含行元素时按行 `\n` 连接，否则取全文；用一个含 `<tspan>` 的最小 SVG 片段在浏览器控制台验证——三行标签返回 `"alpha\nbeta\ngamma"`（不是 `"alphabetagamma"`），单行标签返回其全文
- [x] 1.2 增加剪贴板写入函数：先 `navigator.clipboard.writeText`，失败退化为隐藏 textarea + `document.execCommand('copy')`，整体 `try/catch` 不外抛；在预览里复制一段文字后粘贴到编辑器验证内容正确
- [x] 1.3 在既有 window 级 `click` 监听中接入复制：命中 `text` 元素、且当前选区为折叠时执行复制；单击文字后粘贴验证成功，且拖动平移与角标跳转行为不变

## 2. 交互冲突消解

- [x] 2.1 选区非空时单击不复制：先划选一段文字再单击另一处文字，验证剪贴板内容未变、原选区仍在
- [x] 2.2 确认 `mousedown` 分流无需改动：分别验证在文字上拖动只划选不平移、在框体/空白拖动只平移不划选、平移经过文字时保持抓取光标
- [x] 2.3 验证双击选词仍生效：双击某段文字中的一个词，该词被选中、预览不移动、浏览器控制台无未捕获异常

## 3. 反馈

- [x] 3.1 增加复制成功后的就地高亮样式与清除定时器（不复用 `#toast`），单击文字后验证被复制文字短暂高亮并自动恢复
- [x] 3.2 确认文字上的光标保持文本选择语义不变（不引入复制专用光标），并验证 `is-dragging` 状态下仍为抓取语义（平移经过文字时手型不闪）
- [x] 3.3 在预览正显示编译错误提示时单击文字完成复制，验证高亮出现且错误提示内容仍可见

## 4. 状态隔离与回归

- [x] 4.1 验证复制不影响视图状态：缩放并平移到某位置后单击文字复制，缩放比例与平移位置保持不变、不重新适配
- [x] 4.2 按 `docs/board-navigation.md` §7 的方法用本地 http 服务 + stub `acquireVsCodeApi` 加载 `pages/previewPage.html`，走一遍 render 消息后验证：复制、平移、缩放、场景栏、返回按钮均正常且控制台无异常

## 5. 文档

- [x] 5.1 在 `docs/board-navigation.md` §4 预览实现要点补一条"单击文字复制"的交互说明与 d2 文字渲染结构依赖（含 `<tspan>` 无分隔符拼接陷阱），并说明改动只在 `pages/previewPage.html`、无需重新打包
